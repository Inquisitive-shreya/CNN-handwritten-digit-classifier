# 🧠 Digit Classification using CNN

This project implements a **Digit Classification Model** using a **Convolutional Neural Network (CNN)** to recognize handwritten digits (0–9). The model is trained on the MNIST dataset and can accurately classify input digit images.

---

## 📌 Features
- Handwritten digit recognition (0–9)
- CNN-based deep learning model
- Trained on MNIST dataset
- High accuracy on test data
- Easy-to-use inference script
- Clean and modular code structure

---

## 🛠️ Tech Stack
- Python
- PyTorch / TensorFlow (update based on what you used)
- NumPy
- Matplotlib
- Jupyter Notebook (optional)



## 🧪 Model Architecture
- Convolutional Layer
- ReLU Activation
- Max Pooling
- Fully Connected Layer
- Softmax Output (10 classes)

---

## 🚀 Installation

Clone the repository:
```bash
git clone https://github.com/inquisitive-shreya/digit-classification.git
cd digit-classification